function solve(params) {
    var s = params[0].split(‘ ‘).map(Number),
        result;
    // Your solution here
    console.log(result);
}
function solve(params) {
    var s = params[0].split(‘ ‘).map(Number),
        result;
    // Your solution here
    console.log(result);
}
